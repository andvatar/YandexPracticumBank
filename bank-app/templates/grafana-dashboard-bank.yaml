apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-dashboard-bank
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Bank"
data:
  bank-dashboard.json: |-
{{ (.Files.Get "grafana/bank-dashboard.yaml") | indent 4 }}