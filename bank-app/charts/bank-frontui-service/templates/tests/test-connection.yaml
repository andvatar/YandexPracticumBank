apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-frontui-service.fullname" . }}-test-connection"
  labels:
    {{- include "bank-frontui-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-frontui-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
