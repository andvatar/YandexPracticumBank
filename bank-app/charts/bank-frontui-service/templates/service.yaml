apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-frontui-service.fullname" . }}
  labels:
    {{- include "bank-frontui-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-frontui-service.selectorLabels" . | nindent 4 }}
