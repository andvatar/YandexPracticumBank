apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-accounts-service.fullname" . }}
  labels:
    {{- include "bank-accounts-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-accounts-service.selectorLabels" . | nindent 4 }}
