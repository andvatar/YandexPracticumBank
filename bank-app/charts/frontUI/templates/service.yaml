apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontUI.fullname" . }}
  labels:
    {{- include "frontUI.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontUI.selectorLabels" . | nindent 4 }}
