apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-exchangegenerator-service.fullname" . }}
  labels:
    {{- include "bank-exchangegenerator-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-exchangegenerator-service.selectorLabels" . | nindent 4 }}
