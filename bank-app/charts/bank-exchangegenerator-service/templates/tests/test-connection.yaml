apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-exchangegenerator-service.fullname" . }}-test-connection"
  labels:
    {{- include "bank-exchangegenerator-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-exchangegenerator-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
